unset $SHLVL ; bash ; echo $SHLVL
export $SHLVL=''; bash ; echo $SLVL
echo ~$USER/sdfsfsfdsfs
echo ~framos-p/sdfsfsfdsfs
unset HOME ; echo ~
echo aaa > $HOME.txt > bb (exit code = 1, permission denied error and should not create bb)
echo ana < kdhfsdf > bb (exit code = 2, no file or dir error and bb is not created)
echo $""USER
echo $USER "" $USER '$USER'$ "$USER" "'$USER'$ $USER"'$USER'"$USER"$'"$USER"' "$USER"$nada$1aaa$
unset _ ; env
export PATH (when path already exists)
export PATH='::::::'; minishell
export PATH='::::::'; ls
export PATH=';'; ls
export PATH='/usr/bin'; ls
export PATH='/usr/bin'; whoami
export A='fi le'; echo hola > $A
export 'AH$'=nop\n
